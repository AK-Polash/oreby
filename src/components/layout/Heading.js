import React from "react";

const Heading = ({ title }) => {
  return (
    <h2 className="pb-12 font-dm text-[39px] font-bold capitalize text-primary">
      {title}
    </h2>
  );
};

export default Heading;
